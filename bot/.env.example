# Slack (required for the bot)
# Create an app at https://api.slack.com/apps → Create New App → From scratch
# Add Bot Token Scopes: commands, chat:write
# Install app to your workspace and copy tokens below
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret

# GitHub (required for /meme status and /meme used — read/update batch ledgers)
# Create a fine-grained or classic token at https://github.com/settings/tokens
# Repo permissions: Contents read + write
GITHUB_TOKEN=ghp_your_github_token
REPO_OWNER=Nj-E
REPO_NAME=Workflow-example

# Optional: port for the Bolt app (default 3000)
# PORT=3000
